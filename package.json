{
	"name": "log-sink-server",
	"version": "0.1.0",
	"author": "Sitegui <sitegui@sitegui.com.br>",
	"description": "A log service, for streaming, storage and queries of log data",
	"main": "./index.js",
	"repository": {
		"type": "git",
		"url": "https://github.com/sitegui/log-sink-server"
	},
	"keywords": ["log", "stream", "mongodb"],
	"dependencies": {
		"asynconnection-node": "^0.1.0",
		"commander": "^2.5.1",
		"mongoose": "^3.8.21"
	},
	"license": "MIT",
	"engines": {
		"node": ">=0.10"
	},
	"scripts": {
		"test": "mocha test",
		"docs": "jsdoc -d docs -c docs.json -R README.md",
		"start": "forever start -l forever.log -a -o out.log -e err.log index.js"
	},
	"devDependencies": {
		"jsdoc": "^3.3.0-alpha13",
		"mocha": "^2.1.0",
		"should": "^4.4.1"
	}
}